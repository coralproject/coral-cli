version: 2
updates:
  # Update npm packages if they're found daily.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"

  # Update Github Actions if they're found daily.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
